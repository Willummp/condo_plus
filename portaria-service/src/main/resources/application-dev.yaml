spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/condoplus
    username: postgres
    password: postgres
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    open-in-view: false

condominio:
  service:
    url: http://localhost:8081