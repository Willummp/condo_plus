spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/condoplus
    username: postgres
    password: postgres
  data:
    redis:
      host: redis
      port: 6379
  kafka:
    bootstrap-servers: kafka:9092 # Ajuste para o nome do container do Kafka no seu docker-compose

condominio:
  service:
    url: http://condominio-service:8081