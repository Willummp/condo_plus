package com.condoplus.condominio.convivencia.domain;

public enum CategoriaMulta {
    /** Barulho, descumprimento de regras de convivência — responsabilidade do RESIDENTE (escopo SOCIAL) */
    CONVIVENCIA,

    /** Obras irregulares, dano ao bem comum — responsabilidade do PROPRIETÁRIO (escopo LEGAL) */
    ESTRUTURAL
}
